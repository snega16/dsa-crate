def findIndex(arr, target, index):
    if index == len(arr):
        return -1
    if arr[index] == target:
        return index
    return findIndex(arr, target, index+1)
    
arr = [1,3,8,2,7,4]
print(findIndex(arr, 8, index=0))
